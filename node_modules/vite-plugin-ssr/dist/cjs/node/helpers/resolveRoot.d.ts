export { resolveRoot };
export { assertRoot };
import type { ResolvedConfig, UserConfig } from 'vite';
declare function resolveRoot(config: UserConfig): string;
declare function assertRoot(root: string, config: ResolvedConfig): void;
