export { configDefinitionsBuiltIn };
export type { ConfigDefinition };
import type { ConfigName, c_Env } from '../../../../../shared/page-configs/PageConfig';
declare type ConfigDefinition = {
    c_env: c_Env;
    c_global?: boolean;
    c_required?: boolean;
    c_code?: true;
    c_validate?: (configResolved: ({
        configValue: unknown;
    } | {
        configValue: string;
        codeFilePath: string;
    }) & {
        configFilePath: string;
    }) => void | undefined;
    sideEffect?: (config: {
        configValue: unknown;
        configDefinedBy: string;
    }) => undefined | Record<string, Partial<ConfigDefinition>>;
};
declare type ConfigDefinitionsBuiltIn = Record<ConfigName, ConfigDefinition>;
declare const configDefinitionsBuiltIn: ConfigDefinitionsBuiltIn;
